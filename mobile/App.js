import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

import Routes from './src/routes';

export default function App() {
  return (

     // View é usada para qualquer tipo de container
     // Text é usada para qualquer tipo de texto
     // No react native, não existe herança de estilos

    <Routes />
  );
}